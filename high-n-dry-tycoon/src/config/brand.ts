/**
 * High N' Dry brand tokens.
 *
 * TODO(ben): replace with the real brand hexes. These are placeholders chosen to match the
 * direction in §11.3 — warm sodium-lamp interior against a cool blue-grey street — so the render
 * layer can be built and restyled from one file later.
 */
export const brand = {
  /** TODO(ben): real brand red. */
  red: "#C6402B",
  /** TODO(ben): real brand amber / heat lamp. */
  lamp: "#FF9E1B",
  char: "#241F1C",
  steel: "#A8AFB6",
  /** The cold city outside. */
  street: "#2B333D",
  streetLight: "#3D4855",
  paper: "#F6F1E4",
} as const;
